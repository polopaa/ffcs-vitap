// SlotSelector.tsx is intentionally empty.
//
// Slot selection previously happened here via manual cell-clicking. It has been
// replaced by the credit-based combination dropdown in CourseModal
// (see lib/slotCombinations.ts). No component imports this module anymore; the
// file is retained as an empty placeholder so legacy paths do not break.
export {};
